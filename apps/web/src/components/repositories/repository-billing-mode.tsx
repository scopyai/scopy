export function RepositoryBillingMode({
  workspaceId,
  repositoryId,
  disabled,
}: {
  workspaceId: string
  repositoryId: string
  disabled: boolean
}) {
  void workspaceId
  void repositoryId
  void disabled
  return null
}
